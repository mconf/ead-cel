#include "seq.h"

int embaralhamento(){
/*
    queue_t * queue;
    queue_consumer_t * consumer;

    //inicializações da queue
    queue = queue_create();
    consumer = queue_registerConsumer(queue);
    //inicializações da storage
    net_storage_t * storage;
    storage = net_storageInit(5,queue,NET_CONTENT_TYPE_VIDEO);
    //inicializações da packet
    net_packet_t * packet;
    packet = net_packetNew(1006);

    storage->runningKiller = 0;


    net_packetSetAudioFlags(packet,0);
    net_packetSetBufferSize(packet,3050);
    net_packetSetNumFrag(packet,1);
    net_packetSetNumSeq(packet,1);
    net_packetSetTimestamp(packet,getTimestamp());

    net_storageAdd(storage,packet,1024-18,1024);
    net_packetSetNumFrag(packet,2);
    net_storageAdd(storage,packet,1024-18,1024);
    net_packetSetNumFrag(packet,3);
    net_storageAdd(storage,packet,1024-18,1024);

    net_storagePrint(storage);

    net_packetSetNumSeq(packet,2);
    net_packetSetNumFrag(packet,1);
    net_storageAdd(storage,packet,1024-18,1024);
    net_packetSetNumFrag(packet,2);
    net_storageAdd(storage,packet,1024-18,1024);
net_storagePrint(storage);
     net_packetSetNumSeq(packet,3);
    net_packetSetNumFrag(packet,1);
    net_storageAdd(storage,packet,1024-18,1024);
    net_packetSetNumFrag(packet,2);
    net_storageAdd(storage,packet,1024-18,1024);
net_storagePrint(storage);
     net_packetSetNumSeq(packet,4);
    net_packetSetNumFrag(packet,1);
    net_storageAdd(storage,packet,1024-18,1024);
    net_packetSetNumFrag(packet,2);
    net_storageAdd(storage,packet,1024-18,1024);
net_storagePrint(storage);
     net_packetSetNumSeq(packet,5);
    net_packetSetNumFrag(packet,1);
    net_storageAdd(storage,packet,1024-18,1024);
    net_packetSetNumFrag(packet,2);
    net_storageAdd(storage,packet,1024-18,1024);
net_storagePrint(storage);
     net_packetSetNumSeq(packet,6);
    net_packetSetNumFrag(packet,1);
    net_storageAdd(storage,packet,1024-18,1024);
    net_packetSetNumFrag(packet,2);
    net_storageAdd(storage,packet,1024-18,1024);
   

    net_storagePrint(storage);

    printf("VEja se o tamanho em bytes recebidos do primeiro pacote excedeu 2012.. se sim... está errado!\n");
    */
    return 0;

}